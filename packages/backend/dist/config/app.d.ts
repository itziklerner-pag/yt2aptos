import { Express } from 'express';
import 'express-async-errors';
/**
 * Configure and set up the Express application
 * @returns Configured Express application
 */
export declare function configureApp(): Express;
